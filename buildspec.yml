version: 0.2
phases:
 install:
   commands:
     - echo "install step"
 build:
   commands:
     - npm install
 post_build:
   commands:
     #- rm -rf node_modules
     - aws s3 sync public/ s3://aws-website-devalexwattocouk-wca6c
artifacts:
  files: 
   - 'public/index.html'
