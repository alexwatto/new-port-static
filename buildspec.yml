version: 0.1
phases:
 install:
   commands:
     - echo "install step"
 build:
   commands:
     - npm install gulp -g
     - npm install
     - gulp default
 post_build:
   commands:
     - rm -rf node_modules
     - rm -rf bower_components
     - aws s3 sync --delete . "s3://aws-website-wwwalexwattocouk-nxpfu"
artifacts:
 files:
   - public/*